spring.jpa.show-sql=true

#----------------------------------------------------------------

#db configuration

spring.datasource.url=jdbc:mysql://localhost:3306/blogging_application_api
spring.datasource.username=root
spring.datasource.password=diggy@mysql

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#create, create-drop, update(if tables are not there then create tables otherwise leave unchanged), validate
#[Check these 4 options on Google]
spring.jpa.hibernate.ddl-auto=update

#----------------------------------------------------------------

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

project.image=images/

#----------------------------------------------------------------

spring.security.user.name=digvijay
spring.security.user.password=digvijay
spring.security.user.roles=ADMIN
